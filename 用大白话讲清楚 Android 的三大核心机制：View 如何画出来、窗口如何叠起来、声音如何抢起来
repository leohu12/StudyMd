# Android 核心原理详解：View 绘制、窗口管理、音频焦点

> 用大白话讲清楚 Android 的三大核心机制：View 如何画出来、窗口如何叠起来、声音如何抢起来。

---

## 目录

1. [View 绘制原理](#view-绘制原理)
   - [三步曲：Measure、Layout、Draw](#三步曲measurelayoutdraw)
   - [递归流程详解](#递归流程详解)
   - [自定义 View](#自定义-view)
   - [自定义 ViewGroup](#自定义-viewgroup)
2. [窗口管理原理](#窗口管理原理)
   - [窗口是什么](#窗口是什么)
   - [窗口层级（z-order）](#窗口层级z-order)
   - [触摸事件分发](#触摸事件分发)
   - [多窗口竞合处理](#多窗口竞合处理)
3. [音频焦点原理](#音频焦点原理)
   - [麦克风栈模型](#麦克风栈模型)
   - [四种申请方式](#四种申请方式)
   - [音频策略（AudioPolicy）](#音频策略audiopolicy)
   - [车载多音区](#车载多音区)

---

## View 绘制原理

### 三步曲：Measure、Layout、Draw

Android 的 View 绘制就像装修一间毛坯房，**核心就是三步：量尺寸、摆位置、刷油漆**。

| 步骤 | 对应方法 | 大白话 |
|------|----------|--------|
| **Measure** | `onMeasure()` | 系统问每个控件：“你要占多大地方？” |
| **Layout** | `onLayout()` | 父容器告诉子控件：“你放在哪个位置。” |
| **Draw** | `onDraw()` | 每个控件在自己的区域里把自己画出来。 |

### 递归流程详解

整个 View 树是一棵“树”结构：
- **根**：最外层的 DecorView（ViewGroup）
- **树枝**：中间的 ViewGroup（如 LinearLayout）
- **树叶**：底层的普通 View（如 TextView）

**递归的含义**：每个 ViewGroup 只管理自己的直接孩子，但每个孩子可能是另一个 ViewGroup，于是层层传递。

#### Measure 递归过程

根 ViewGroup.onMeasure()
├── 遍历直接孩子
│ ├── 对子 ViewGroup 调用 measure()
│ │ └── 子 ViewGroup.onMeasure()
│ │ └── 遍历它的孩子（叶子 View）
│ │ └── 叶子 View 测量自己
│ └── 对叶子 View 调用 measure()
└── 根据所有孩子的尺寸，确定自己的尺寸

#### Layout 递归过程
根 ViewGroup.layout()
└── onLayout() 遍历直接孩子
├── 计算每个孩子的位置
├── 对子 ViewGroup 调用 layout()
│ └── 子 ViewGroup.onLayout()
│ └── 继续为它的孩子安排位置
└── 对叶子 View 调用 layout()
└── 叶子 View 记录自己的坐标


#### Draw 递归过程
根 ViewGroup.draw()
├── 画自己的背景
├── 画自己的内容
└── dispatchDraw() 遍历直接孩子
├── 对子 ViewGroup 调用 draw()
│ └── 子 ViewGroup 重复上述流程
└── 对叶子 View 调用 draw()
└── 叶子 View.onDraw() 画自己

text

### 自定义 View

自定义 View 是做一个**独立的控件**，不包含子 View。

**核心方法**：
- `onMeasure()`：告诉系统你要多大
- `onDraw()`：画出你的样子

**示例：一个红色的圆形 View**

```java
public class CircleView extends View {
    private Paint paint;

    public CircleView(Context context, AttributeSet attrs) {
        super(context, attrs);
        paint = new Paint();
        paint.setColor(Color.RED);
        paint.setStyle(Paint.Style.FILL);
    }

    @Override
    protected void onMeasure(int widthMeasureSpec, int heightMeasureSpec) {
        int defaultSize = dpToPx(100);
        int width = MeasureSpec.getMode(widthMeasureSpec) == MeasureSpec.EXACTLY 
                ? MeasureSpec.getSize(widthMeasureSpec) : defaultSize;
        int height = MeasureSpec.getMode(heightMeasureSpec) == MeasureSpec.EXACTLY 
                ? MeasureSpec.getSize(heightMeasureSpec) : defaultSize;
        int size = Math.min(width, height);
        setMeasuredDimension(size, size);
    }

    @Override
    protected void onDraw(Canvas canvas) {
        int radius = Math.min(getWidth(), getHeight()) / 2;
        canvas.drawCircle(getWidth() / 2f, getHeight() / 2f, radius, paint);
    }

    private int dpToPx(int dp) {
        return (int) (dp * getResources().getDisplayMetrics().density);
    }
}
自定义 ViewGroup
自定义 ViewGroup 是做一个可以装子 View 的容器，负责测量和摆放孩子。

核心方法：

onMeasure()：先测量所有子 View，再确定自己多大

onLayout()：给每个子 View 分配位置

示例：一个垂直排列的简单布局

java
public class SimpleVerticalLayout extends ViewGroup {
    public SimpleVerticalLayout(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    @Override
    protected void onMeasure(int widthMeasureSpec, int heightMeasureSpec) {
        int totalHeight = 0;
        int maxWidth = 0;
        for (int i = 0; i < getChildCount(); i++) {
            View child = getChildAt(i);
            measureChild(child, widthMeasureSpec, heightMeasureSpec);
            totalHeight += child.getMeasuredHeight();
            maxWidth = Math.max(maxWidth, child.getMeasuredWidth());
        }
        totalHeight += getPaddingTop() + getPaddingBottom();
        maxWidth += getPaddingLeft() + getPaddingRight();
        setMeasuredDimension(
            resolveSize(maxWidth, widthMeasureSpec),
            resolveSize(totalHeight, heightMeasureSpec)
        );
    }

    @Override
    protected void onLayout(boolean changed, int l, int t, int r, int b) {
        int currentTop = getPaddingTop();
        for (int i = 0; i < getChildCount(); i++) {
            View child = getChildAt(i);
            int left = getPaddingLeft();
            int right = left + child.getMeasuredWidth();
            int bottom = currentTop + child.getMeasuredHeight();
            child.layout(left, currentTop, right, bottom);
            currentTop = bottom;
        }
    }
}
窗口管理原理
窗口是什么
窗口就是一个独立的画板：

每个 Activity 有一块画板

对话框、Toast、状态栏、输入法也都有自己的画板

这些画板在屏幕上叠在一起，靠前的挡住靠后的

核心服务：

WindowManagerService (WMS)：舞台总指挥，掌握所有窗口的层次顺序、位置、大小

SurfaceFlinger：灯光合成师，把所有窗口的图像合成到一起，最终显示在屏幕上

窗口层级（z-order）
系统用类型来决定层级，数字越大越在上面：

类型	层级范围	示例
应用窗口	1～99	Activity
子窗口	1000～1999	对话框、PopupWindow
系统窗口	2000～2999	状态栏、输入法、Toast、锁屏
叠放顺序永远是：

text
锁屏 > 状态栏/导航栏 > 输入法 > 系统悬浮窗 > 对话框 > Activity > 桌面
触摸事件分发
你手指点下去时，WMS 会从最顶层的窗口开始，逐层向下询问：

这个点落在你的区域里吗？

你愿意处理这个事件吗？

规则：最上面的、可触摸的窗口优先处理，事件不会往下传。

多窗口竞合处理
多个窗口同时存在时，系统通过三件事维持秩序：

固定层级表：按窗口类型决定谁一定在上面

自上而下的事件分发：最上层窗口优先处理触摸

单一焦点：只有一个窗口能接收键盘等输入

车载系统的特殊处理：

安全关键窗口（倒车影像、碰撞预警）具有最高优先级

支持多屏独立（中控、仪表盘、后排娱乐各管各的）

支持多音区隔离（驾驶区、副驾区、后排区窗口独立）

音频焦点原理
麦克风栈模型
整个系统只有一支麦克风，谁拿到谁说话。

系统内部维护着一个栈：

新应用申请 → 压入栈顶，拿到焦点

老应用被顶替 → 下沉到栈中第二的位置，收到“失去焦点”通知

释放焦点 → 弹出栈顶，下一个应用自动拿到焦点

四种申请方式
申请类型	大白话	对其他App的通知	结束后
GAIN	我要开演唱会，用很久	永久失去 (LOSS)	不自动恢复
GAIN_TRANSIENT	我插一句话，说完就走	暂时失去 (LOSS_TRANSIENT)	自动恢复
GAIN_TRANSIENT_MAY_DUCK	我小声嘀咕，你们继续	闪避 (CAN_DUCK)	自动恢复音量
GAIN_TRANSIENT_EXCLUSIVE	我要清场，谁也别出声	暂时失去且独占	自动恢复
为什么需要区分？

开演唱会：音乐App播放歌曲，把播客彻底顶掉

插话：语音助手说一句话，说完音乐继续

小声嘀咕：导航播报，音乐降低音量当背景

清场：微信语音消息，录完之前播的内容继续

音频策略（AudioPolicy）
如果只有麦克风，大家只能争抢一个话筒，但实际场景要复杂得多：

车上有好几个喇叭（驾驶座、后排、副驾）

导航播报和音乐不能同时从同一个喇叭出来

打电话时，后排可能还想看电影

AudioPolicyService 就是“总导演”，手里有一本剧本：

什么节目该用哪个喇叭

什么节目优先级高

音量该怎么配合

策略注入：车厂可以现场改剧本，通过 registerAudioPolicy() 注入自定义规则，实现“导航强制静音音乐”“后排独立音量”等定制功能。

车载多音区
高级车载系统里，车内有多个“音区”：

驾驶区：导航、通话

副驾区：视频、音乐

后排区：游戏、电影

每个音区有独立的麦克风栈，互不干扰。后排看动画片时，驾驶区的导航播报不会影响他们。

总结
机制	核心问题	解决方案
View绘制	控件怎么画出来	Measure → Layout → Draw 递归流程
窗口管理	多个窗口怎么叠	按类型分配层级（z-order），从上到下分发事件
音频焦点	多个声音怎么抢	麦克风栈模型，四种申请方式表达意图
音频策略	声音往哪个设备走	配置文件定义规则，系统App可注入自定义策略
三个机制的共同点：都是资源调度系统，依靠优先级、栈结构、策略配置来保证关键任务优先。
